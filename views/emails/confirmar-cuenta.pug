<!DOCTYPE html>
html(lang="es")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body
        h2 Confirma tu cuenta ControlTask
        p Ya estás casi inscrito, solo presiona el siguiente botón.
        a(href=`${confirmarEmail}`) Ir a confirmar la contraseña. 
        p Si no puedes acceder a este enlace, visita #{confirmarEmail}
        p Si no solicitaste este email, puedes ignorarlo.

        