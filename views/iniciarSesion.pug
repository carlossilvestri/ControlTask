<!DOCTYPE html>
html(lang="es")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Inicia Sesión en Uptask
            include partials/mensajeDeError
            form#formulario.caja.caja-login(method="POST" action="/iniciar-sesion")
                .campo
                    label(form="email") Email:
                    input(type="email" name="email" placeholder="Tu Email" required value= email)
                .campo
                    label(form="password") Password:
                    input(type="password" name="password" placeholder="Tu password" required value= password)
                .campo.enviar
                    input.boton(type="submit" value="Inicia Sesión")
                .campo.acciones
                    a(href="/crear-cuenta") ¿Aún no tienes cuenta? Crea una gratis.
                    a(href="/reestablecer") ¿Olvidaste tu contraseña?

        script(src="/dist/bundle.js")